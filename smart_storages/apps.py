"""Django app configuration for smart_storages."""
from django.apps import AppConfig


class SmartStoragesConfig(AppConfig):
    """Configuration for the Smart Storages app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "smart_storages"
    verbose_name = "Smart Storages"
